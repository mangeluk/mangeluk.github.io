# .github/workflows/deploy.yml
# CI/CD pipeline: build Next.js static export and publish to GitHub Pages.
# Requirements: 21.1–21.6

name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Build Next.js static export
        run: npm run build
        env:
          NEXT_PUBLIC_GEMINI_KEY: ${{ secrets.GEMINI_KEY }}

      # If build fails, this step is skipped → no broken content published (Req. 21.4)
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./out
